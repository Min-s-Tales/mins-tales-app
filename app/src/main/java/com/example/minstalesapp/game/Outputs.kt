package com.example.minstalesapp.game

/**
 * The different types of Outputs
 */
enum class Outputs {
    NARRATOR, //Must be used only once at a time
    MUSIC, //Should be used once with loop enabled
    SURROUNDINGS //Multiple surroundings can be used at the same time
}