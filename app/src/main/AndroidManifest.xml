<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.minstalesapp">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_mins_tales"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_mins_tales_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MinsTalesApp"
        android:usesCleartextTraffic="true">
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListStoryActivity"/>
        <activity android:name=".ProfileActivity"/>
        <activity android:name=".StoryPlayerActivity"/>
        <activity android:name=".MarketplaceActivity"/>
        <activity android:name=".RecordActivity"/>
        <activity android:name=".LoginActivity"/>
        <activity android:name=".SignupActivity"/>
    </application>
</manifest>